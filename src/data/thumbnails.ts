export type ThumbnailItem = {
  img?: string;
  title: string;
  platform?: string;
  views?: string;
  channel?: string;
  videoUrl?: string;
  context?: string;
  tags?: string[];
};

export const THUMBNAIL_ITEMS: ThumbnailItem[] = [];
