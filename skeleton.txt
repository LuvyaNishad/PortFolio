aurelius-portfolio/
│
├── public/
│   ├── favicon.ico
│   ├── og-image.png                    # Open Graph preview (1200×630)
│   ├── resume.pdf                      # Your resume — linked from nav
│   └── fonts/                          # Optional: self-host fonts here
│
├── src/
│   │
│   ├── main.jsx                        # ReactDOM.createRoot entry point
│   ├── App.jsx                         # Root component — mounts Loader + Portfolio
│   │
│   ├── styles/
│   │   └── globals.css                 # @import fonts, CSS vars, keyframes, scrollbar, .glass, .serif
│   │
│   ├── data/                           # All your content lives here — no touching components
│   │   ├── thumbnails.js               # THUMBNAIL_ITEMS array
│   │   ├── videos.js                   # VIDEO_ITEMS array
│   │   ├── graphics.js                 # GRAPHIC_ITEMS array
│   │   ├── codeProjects.js             # CODE_ITEMS array
│   │   ├── navLinks.js                 # NAV_LINKS + GITHUB_USERNAME
│   │   └── toolCategories.js           # TOOL_CATEGORIES + Icons
│   │
│   ├── components/
│   │   │
│   │   ├── ui/                         # Reusable primitives
│   │   │   ├── Reveal.jsx              # Reveal + REVEAL_VARIANTS
│   │   │   ├── SLabel.jsx              # ✦ Section label
│   │   │   ├── GlassFilter.jsx         # SVG liquid-glass filter def
│   │   │   ├── LiquidButtonPrimary.jsx # Green filled CTA
│   │   │   ├── LiquidButtonGhost.jsx   # Frosted outline CTA
│   │   │   ├── LiquidChip.jsx          # Social pill chip
│   │   │   └── AddCard.jsx             # Dashed placeholder card
│   │   │
│   │   ├── layout/
│   │   │   ├── Background.jsx          # Animated fold gradient + canvas trail
│   │   │   ├── CustomCursor.jsx        # Dot + ring cursor
│   │   │   ├── Nav.jsx                 # Floating pill nav + lamp + scroll-spy
│   │   │   └── Footer.jsx              # Footer with links + copyright
│   │   │
│   │   ├── loader/
│   │   │   └── Loader.jsx              # Split curtain loader + letter stagger + counter
│   │   │
│   │   └── sections/
│   │       ├── Hero.jsx                # Hero headline + CTAs + scroll cue
│   │       ├── About.jsx               # Photo card + pull quote + bio + pills
│   │       ├── Tools.jsx               # Marquee ticker rows + MarqueeRow + ToolChip + Dot
│   │       ├── Library.jsx             # 4-card index grid
│   │       ├── work/
│   │       │   ├── WorkSection.jsx     # Shared shell — bleed number + ghost text + heading
│   │       │   ├── Thumbnails.jsx      # ThumbnailCard + grid
│   │       │   ├── VideoEdits.jsx      # VideoCard + grid
│   │       │   ├── GraphicDesign.jsx   # GraphicCard + grid
│   │       │   └── CodeProjects.jsx    # CodeCard + STACK_COLORS + grid
│   │       └── Contact.jsx             # Inline form + email + socials
│   │
│   └── hooks/                          # Optional custom hooks
│       ├── useScrollSpy.js             # IntersectionObserver scroll-spy logic (extracted from Nav)
│       └── useCursorTrail.js           # Canvas rAF trail logic (extracted from Background)
│
├── index.html                          # Vite HTML shell
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
├── package.json
├── .gitignore
└── README.md                           # Explain the project, stack choices, how to add work